export enum ButtonIcon {
  hand = "hand",
  one_finger = "one_finger",
  two_fingers = "two_fingers",
  arrow = "arrow",
}

export enum ButtonType {
  primary = "primary",
  secondary = "secondary",
}
